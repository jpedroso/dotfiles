{{ $name := promptString "name" -}}
{{ $email := promptString "email" -}}
{{ $work := promptBool "install work packages" }}

[data]
  work = {{ $work }}
  email = {{ $email | quote }}
  name = {{ $name | quote }}
